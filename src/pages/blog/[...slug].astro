---
import { type CollectionEntry, getCollection } from "astro:content";
import { render } from "astro:content";

import Layout from "../../layouts/Type240.astro";
import Blog from "../../components/blog/Blog.astro";
import BlogHeader from "../../components/blog/BlogHeader.astro";

export async function getStaticPaths() {
  const posts = await getCollection("blog");
  return posts.map((post) => ({
    params: { slug: post.id },
    props: post,
  }));
}
type Props = CollectionEntry<"blog">;

const post = Astro.props;
const postDate = new Date(post.data.date);
const currentMonth = postDate.getMonth();
const currentYear = postDate.getFullYear();
---

<Layout isTop={false} title={post.data.title} slug={post.id}>
  <article class="article -v">
    <Blog post={post} isFirst={true} />
  </article>
</Layout>
